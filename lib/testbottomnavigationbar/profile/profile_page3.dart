import 'package:flutter/material.dart';

class ProfilePage3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Profile Page 3")),
      body: Center(
        child: Text("This is Profile Page 3"),
      ),
    );
  }
}
